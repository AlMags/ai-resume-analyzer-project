[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-resume-analyzer"
version = "0.1.0"
description = "AI Resume Analyzer built with Google Gemini"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["app"]